<h2>Appointments of <%= @doctor.name %> : </h2>
<ul>
  <%= @doctor.appointments.each do |a| %>
    <% date = DateTime.parse(a.appointment_datetime) %>
    <li> <%=date.strftime("%B %d, %Y at %H:%M") %></li>
  <%end%>
</ul>
